{
  "name": "maketrades-space",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run generate:seo",
    "generate:seo": "node scripts/seo-build.mjs",
    "generate:sitemap": "node scripts/seo-build.mjs --sitemap-only",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.3.3"
  }
}
